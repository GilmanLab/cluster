classes:
  - chart

parameters:
  helm:
    chart:
      url: https://kubernetes-charts.storage.googleapis.com/nfs-client-provisioner-1.2.8.tgz
      folder_name: nfs-client-provisioner
    values:
      nfs:
        server: ${environment:nfs:address}
        path: ${environment:nfs:path}
      replicaCount: 3
      storageClass:
        name: nfs